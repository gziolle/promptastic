<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/widget_background_color"
    android:padding="@dimen/widget_margin">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="@dimen/widget_title_height"
        android:text="@string/app_name"
        android:gravity="center_vertical"
        android:paddingStart="@dimen/activity_default_margin"
        android:paddingEnd="@dimen/activity_default_margin"
        android:textSize="18sp"
        android:textStyle="bold"
        android:background="@color/colorPrimary"/>

    <TextView
        android:id="@+id/tv_widget_last_played_script"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/colorPrimaryText"
        android:gravity="center_horizontal"
        android:layout_marginTop="@dimen/widget_double_text_margin"
        android:layout_marginBottom="@dimen/widget_text_margin"
        android:textSize="@dimen/widget_text_size"/>

    <TextView
        android:id="@+id/tv_widget_script_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:textColor="@color/colorPrimaryText"
        android:textSize="@dimen/widget_text_size"/>

</LinearLayout>